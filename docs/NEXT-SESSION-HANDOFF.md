# Продолжение работы над claude-tg-bot

Это сообщение для новой сессии. Проект: Telegram-бот на `/Users/evgeniy/projects/claude-tg-bot/`. Только что закончилась длинная сессия — читай `CLAUDE.md` и `HANDOFF.md` (особенно секцию «Сессия 2026-05-06 (вечер)») прежде чем делать что-либо.

## Текущее состояние

Боевой бот `@proboiAI_bot` работает на jinru (5.223.82.96) — не трогать. Тестовый бот `@ORCH7_bot` поднят на новом сервере proboi-bot (89.167.125.175, Hetzner cx33, Helsinki). На нём: Let's Encrypt TLS для proboi.site/dash./design./www., Open Design на `https://design.proboi.site/`, dashboard на `https://dash.proboi.site/`. Доступ к тестовому боту: owner (292228713) + тестер 5615267984. Тестовый токен уже в `.env` на новом сервере — повторно запрашивать не нужно.

SANDBOX ADMIN блок добавлен в `buildOwnerSafetyPrompt()` (`src/config.ts`) — убирает галлюцинацию «не могу писать вне workspace». Путь к `users.json` на новом сервере: `/opt/claude-tg-bot/system/users.json`.

## Следующие задачи по приоритету

**1. Дымовой тест @ORCH7_bot (сделать первым)**
Попросить owner написать боту: прочитать `.env`, отредактировать тестовый файл, убедиться что SANDBOX ADMIN реально убрал ограничения. Без этого теста двигаться дальше рискованно.

**2. Публичные папки `/u/{userId}/`**
nginx на новом сервере уже роутит `/u/` на `/var/www/u/` — каталоги не созданы, квота не реализована. Реализовать по варианту Y: пользователь публикует файл командой боту → бот копирует в `/var/www/u/{userId}/` → доступно по `https://proboi.site/u/{userId}/имя_файла`. Квота: 2 ГБ на пользователя, проверять при каждой записи. Показывать использовано/всего на дашборде (рядом с токенами и ресурсами контейнера).

**3. Prod-миграция (последний шаг)**
Остановить бота на jinru → запустить `scripts/migrate-jinru-to-proboi.sh` → проверить vault'ы гостей перенеслись → заменить `TELEGRAM_BOT_TOKEN` на боевой в `.env` на новом сервере → рестарт → убедиться что `@proboiAI_bot` ожил на новом железе → выключить systemd на jinru.

## Правила

- Никогда не деплоить на сервер без явного подтверждения owner'а (правило `feedback_no_deploy.md`).
- Нетривиальные задачи делегировать Sonnet через Agent — Opus только оркестрирует (`feedback_delegate_to_agents.md`).
- Не трогать jinru пока идёт тест на новом сервере.
- Не читать и не коммитить `.env` файлы (содержат секреты).
