# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# user-specific MCP config (use mcp-config.example.ts as template)
mcp-config.ts

# token usage metering database (SQLite + WAL/SHM sidecars)
metering.sqlite
metering.sqlite-shm
metering.sqlite-wal

# DeepSeek API key pool (per-host, never commit)
system/deepseek-keys.json

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Cursor
.cursor

# Finder (MacOS) folder config
.DS_Store
/claude-bot-standalone

# graphify (local knowledge-graph experiments, never committed)
graphify-input/
graphify-out/

# Playwright MCP cache (browser session data)
.playwright-mcp/

# Runtime / per-host data
workspace/
.planning/

# Claude Code local state (worktrees, settings)
.claude/
